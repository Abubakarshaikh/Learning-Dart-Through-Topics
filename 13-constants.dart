// finalant
// mutable data & immutable data
// final constants & runtime constant.
//
void main() {
  const valueInterger = 90;
  const isRight = false;
  const valueDouble = 3.4;
  const personAge = 45;
  final dateTime = DateTime.now();

  // Call data:
  print(dateTime);
}
